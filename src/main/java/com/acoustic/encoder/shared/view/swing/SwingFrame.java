package com.acoustic.encoder.shared.view.swing;

import javax.swing.*;

public class SwingFrame extends JFrame {

    public SwingFrame(String title, int windowWidth, int windowHeight) {
        super(title);
        this.setSize(windowWidth, windowHeight);
    }

}
